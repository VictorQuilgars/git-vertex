<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
  <!-- push — Git Vertex icon set. Grid 24, stroke 1.7, round caps.
       download turned around: the same arrow and the same baseline.
       EDIT THIS FILE to change the icon — components/Icon reads the folder.
       Do not put stroke-width on a shape: the component sets it. -->
  <path d="M12 20.5V9"/>
  <path d="M8 12.8l4-4 4 4"/>
  <path d="M5 4h14"/>
</svg>
