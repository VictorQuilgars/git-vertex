<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
  <!-- card — interface furniture: a sheet with something written on it, which
       is what a reference's card is. Deliberately NOT `info`: that one is a
       ring around a stroke and a dot, and the forge's issue mark is a ring
       too — three rings on one row is three of the same thing.
       EDIT THIS FILE to change the icon — components/Icon reads the folder.
       Do not put stroke-width on a shape: the component sets it. -->
  <rect x="3.5" y="5" width="17" height="14" rx="2.5"/>
  <path d="M7.5 9.5h9M7.5 13h9M7.5 16h5"/>
</svg>
