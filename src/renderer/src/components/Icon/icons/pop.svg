<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
  <!-- pop — Git Vertex icon set. Grid 24, stroke 1.7, round caps.
       stash with the arrow reversed — out of the box rather than into it.
       EDIT THIS FILE to change the icon — components/Icon reads the folder.
       Do not put stroke-width on a shape: the component sets it. -->
  <rect x="4.5" y="13" width="15" height="7" rx="1.5"/>
  <path d="M10 16.5h4"/>
  <path d="M12 4v5.5"/>
  <path d="M9.8 6.2L12 4l2.2 2.2"/>
</svg>
