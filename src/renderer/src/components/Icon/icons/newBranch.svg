<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
  <!-- newBranch — Git Vertex icon set. Grid 24, stroke 1.7, round caps.
       the graph's fork, but the second branch ends in a plus rather than a
       ring: a branch that does not exist yet.
       EDIT THIS FILE to change the icon — components/Icon reads the folder.
       Do not put stroke-width on a shape: the component sets it. -->
  <path d="M8 6.5v13M8 13c0-5 8-3 8-6.5"/>
  <circle cx="8" cy="4.5" r="1.9"/>
  <circle cx="8" cy="19.5" r="1.9" fill="currentColor" stroke="none"/>
  <path d="M16 2v5M13.5 4.5h5"/>
</svg>
