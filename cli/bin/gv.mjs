#!/usr/bin/env node
// Published launcher: runs the compiled TUI (built to ../dist by `npm run build`).
// For development without a build, use `npm start` (runs the TS sources via tsx).
import '../dist/cli.js'
